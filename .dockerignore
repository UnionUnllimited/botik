.git
.gitignore
.venv
venv
__pycache__
*.py[cod]
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov
.env
backups
logs
deploy/certbot
docs
README.md

# Сторонний продукт: бот и его админка. Они ставятся отдельным процессом
# на хосте со своим venv, наши контейнеры их не импортируют вовсе. Это 10 МБ
# и 369 файлов, каждый из которых COPY ещё и переназначает владельцем —
# отсюда была минута на слой при каждой сборке.
bot

# В образ не едет и остальное, что нужно только на разработке и выкате.
tests
deploy
build
.claude
.github
*.md

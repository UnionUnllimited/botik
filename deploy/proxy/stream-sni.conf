# Разделение 443 по SNI на titanvps.pro (5.34.214.243).
#
# На этом сервере живёт remnanode и держит 443 целиком: на неузнанный
# запрос она отдаёт камуфляж — сертификат amd.com. Отобрать у неё порт
# нельзя, на нём клиенты. Поэтому впереди встаёт nginx, читает SNI
# и разводит: titanvps.pro — витрине, всё остальное — ноде, как было.
#
# Записи в DNS при этом не трогаются вовсе: titanvps.pro уже резолвится
# на этот адрес. Меняется не то, куда ведёт имя, а то, кто отвечает
# на запрос с этим именем.
#
# Кладётся в /etc/nginx/stream.d/, а в /etc/nginx/nginx.conf на верхнем
# уровне — РЯДОМ с http {}, а не внутри него:
#
#   stream {
#       include /etc/nginx/stream.d/*.conf;
#   }
#
# Нужен модуль ngx_stream_ssl_preread — он есть в пакетах nginx и
# nginx-full из репозиториев Debian и Ubuntu.
#
# ВАЖНО: ssl_preread только читает приветствие клиента, не расшифровывая
# его. Трафик ноды идёт насквозь, Reality не ломается — для него это
# такой же прозрачный TCP, как раньше.

map $ssl_preread_server_name $backend_443 {
    # Витрина — наша, её терминируем сами.
    titanvps.pro   shop;

    # Нода Remnawave. Её имя вписать сюда, когда будет известно, — пока
    # клиенты к ней с 443 не попадают, она отвечает только на 9443.
    # <имя-ноды>   remnanode;

    # Всё прочее — x-ui: у неё камуфляж под www.amd.com, и незнакомое имя
    # должно вести себя ровно как раньше. Умолчание не «наше» намеренно:
    # иначе случайный сканер увидел бы витрину там, где раньше был чужой
    # сайт, и сервер стал бы заметен.
    default        xui;
}

upstream shop {
    # Промежуточный хоп, он добавляет PROXY-протокол. 8443 не берём:
    # его на этой машине держит панель x-ui.
    server 127.0.0.1:8542;
}

upstream xui {
    # Инбаунд x-ui после переезда с 443. Клиенты этого не видят: они
    # приходят на 443 к nginx, а он отдаёт их сюда по имени www.amd.com.
    server 127.0.0.1:8444;
}

upstream remnanode {
    # Инбаунд ноды Remnawave. Порт задан в её панели.
    server 127.0.0.1:9443;
}

server {
    listen 443 reuseport;
    listen [::]:443 reuseport;

    ssl_preread on;
    proxy_pass $backend_443;

    # Нода держит длинные соединения, обрывать их нельзя.
    proxy_timeout 10m;
    proxy_connect_timeout 5s;
}

# Промежуточный хоп только для нашего трафика: добавляет PROXY-протокол,
# чтобы витрина видела адрес клиента, а не 127.0.0.1. Прямо в блоке выше
# его включить нельзя — `proxy_protocol` не принимает переменную, и нода,
# которая его не понимает, получила бы мусор в первых байтах и порвала
# бы все соединения разом.
server {
    listen 127.0.0.1:8542;
    proxy_pass 127.0.0.1:8543;
    proxy_protocol on;
    proxy_timeout 75s;
}

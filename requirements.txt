# Runtime-зависимости. Версии проверены по PyPI 03.08.2026, пины жёсткие.
# Обновлять только осознанно: см. docs/decisions.md.

# --- Telegram ---
aiogram==3.30.0
aiohttp==3.14.3            # aiogram 3.30 требует <3.15
magic-filter==1.0.12

# --- Хранилища ---
redis[hiredis]==7.4.1      # aiogram 3.30 (extra "redis") требует redis<8 — не поднимать до 8.x
SQLAlchemy[asyncio]==2.0.51
alembic==1.18.5
asyncpg==0.31.0
greenlet==3.5.4

# --- Web / API ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
jinja2==3.1.6
itsdangerous==2.2.0
python-multipart==0.0.32
httpx==0.28.1

# --- Конфиг / утилиты ---
pydantic==2.13.4           # aiogram 3.30 требует pydantic<2.14
pydantic-settings==2.14.2
python-dotenv==1.2.2
orjson==3.11.9
tenacity==9.1.4
tzdata==2026.3            # IANA-база для zoneinfo: не полагаемся на tzdata хост-системы
phonenumbers==9.0.36
APScheduler==3.11.3

# --- Доступ к роутерам ---
asyncssh==2.24.0        # SSH к роутерам через visitor-туннель

# --- Безопасность ---
cryptography==50.0.0
argon2-cffi==25.1.0
pyotp==2.10.0
qrcode==8.2

# --- Наблюдаемость ---
structlog==26.1.0
prometheus-client==0.26.0
sentry-sdk==2.66.1

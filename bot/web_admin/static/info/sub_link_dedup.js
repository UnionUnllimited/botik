/**
 * Справка: балансировка текстовых ключей (sub_link_dedup).
 * @see link_dedup_grouping.py
 */
(function (R) {
  R.sub_link_dedup = {
    title: 'Балансировка текстовых ключей',
    body:
      '<p>Работает только для <strong>текстовой подписки</strong> — ключи вида ' +
      '<code class="tw-kbd">vless://…</code>, <code class="tw-kbd">vmess://…</code> и т.п. ' +
      'JSON-подписка (<code class="tw-kbd">xrayConfig</code>) не меняется.</p>' +
      '<p><strong>Пример.</strong> На трёх серверах в панели имена ' +
      '<em>🇪🇺 Евро WIFI 1</em>, <em>🇪🇺 Евро WIFI 2</em>, <em>🇪🇺 Евро WIFI 3</em> — ' +
      'три разных ключа с одной «группой» имени. При каждом обновлении подписки из этого пула ' +
      'случайно выбирается <strong>один</strong> ключ, а в списке у клиента остаётся одна строка ' +
      '<em>🇪🇺 Евро WIFI</em> (без цифры в конце) — то есть один из трёх серверов, но не все сразу.</p>' +
      '<ul>' +
      '<li>Цифры <code>1 / 2 / 3</code> в конце имени Remnawave склеиваются в один пул балансировки.</li>' +
      '<li>Теги <code>[PC]</code> / <code>[MOBILE]</code> — отдельные пулы (не смешиваются друг с другом и с «общими»).</li>' +
      '<li>Режим <strong>random</strong> — при следующем запросе подписки может выпасть другой сервер из того же пула.</li>' +
      '<li>Режим <strong>online</strong> — балансировщик опрашивает Remnawave: из пула отбрасываются offline и перегруженные ' +
      '(выше порога). Среди оставшихся выбор <strong>случайный, но с приоритетом менее загруженным</strong>: ' +
      'чем больше «запас» до порога (<code>порог − usersOnline</code>), тем выше шанс. ' +
      'Пример: порог 90, на нодах 50 и 80 онлайн → ~79% / ~21%, не 50/50.</li>' +
      '<li>Фильтр по онлайну: в Remnawave у хоста должна быть привязана нода ' +
      '(<strong>Хосты → Редактировать → Хост ноды</strong>). Без ноды хост не ограничивается по нагрузке.</li>' +
      '</ul>',
    images: ['keybal.png'],
    imageAlt: 'Схема балансировки текстовых ключей',
  };
})(window.AdminInfoRegistry = window.AdminInfoRegistry || {});
